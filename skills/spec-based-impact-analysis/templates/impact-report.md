<!--
============================================================
影響レポートテンプレート（Issue / PR コメント用）
用途: 影響範囲分析（procedure.md）の出力形式。
      元 Issue / PR のコメントとして投稿し、人間判定もコメント上で行う。
      リポジトリ内に分析ログファイルは作らない。
規約:
- confirmed / candidates / unknowns の 3 区分を混ぜない
- candidates は上限 20 行（超過分は追加探索クエリとして提示）
- 証拠は再確認可能な locator（path:line / symbol / test名 / diff hunk）で書く
- 確信度: High = 宣言(trace) + 現存確認済み / Medium = 宣言のみ or 証拠のみ
============================================================
-->

## 影響範囲分析レポート `[impact-analysis]`

- 分析モード: requirement-first | diff-first
- 起点: SPEC-<AREA>-<NNN>（または route / symbol / event 等の起点記述）
- 変更要求: <1 行要約> / 変更種別: <仕様変更|要件追加|バグ修正|UI変更|計測変更|権限変更>
- 手順 version: procedure.md@<git sha 先頭7桁>

### 1. Confirmed impacts（確認済み影響 — 対応が必要）

| # | 影響対象 (locator) | 影響種別 | 確信度 | severity | verification gap | 影響機序 | 証拠 | 推奨確認 | owner | 判定 |
|---|---|---|---|---|---|---|---|---|---|---|
| C1 | `src/...:12` | コード | High | High | テストなし | <なぜ影響するか 1 行> | `@spec` 一致 + 現存確認 | <誰が何を確認するか> | frontend | ⬜ |

### 2. Candidates（候補 — 要判定。上限 20 行）

| # | 影響対象 (locator) | 影響種別 | 確信度 | 影響機序 | 証拠 | 判定 | 棄却理由 |
|---|---|---|---|---|---|---|---|
| A1 | | | Medium | | | ⬜ | |

**上限超過時の追加探索クエリ:** <rg パターン等>

### 3. Unknowns（未確認 — 推測のみ。裏付けなし）

<!-- 意味的推測。recall のためすべて残すが、判定必須ではない -->

- U1: <推測される影響先と理由 1 行>

### 4. テスト不足

| 対象仕様・要求 ID | 現状 | 推奨 |
|---|---|---|
| SPEC-<AREA>-<NNN>.R2 | リンクされたテストなし | <追加すべきテスト観点> |

### 5. 自己批判（この分析が見落としていそうな領域）

<!-- 変化軸 4 つ（LTR/RTL・タブレット・権限・フラグ）、イベントの意味変化、fork 境界、データ後方互換を必ず一度疑う -->

---

**人間判定の記入方法**: 各行の「判定」を ✅ 承認 / ❌ 棄却 / ⏸ 保留 に置き換え、棄却は棄却理由列に 1 行で記入。判定済みレポートはこのコメントが記録の正本になる（`[impact-analysis]` マーカーで後から収集可能）。
